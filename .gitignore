# ==============================================
# 1. Python 编译与缓存文件（自动生成，无需提交）
# ==============================================
__pycache__/          # Python 字节码缓存目录
*.py[cod]             # 编译后的 .pyc, .pyo, .pyd 文件
*$py.class            # Jython 编译文件

# ==============================================
# 2. 虚拟环境与依赖相关（本地环境，无需共享）
# ==============================================
venv/                 # 虚拟环境目录（常见命名）
.venv/                # 虚拟环境目录（另一种命名）
env/                  # 虚拟环境目录（通用命名）
.env/                 # 虚拟环境目录（小写命名）
*.egg-info/           # 打包相关目录
dist/                 # 打包输出目录
build/                # 构建目录

# ==============================================
# 3. 敏感信息与本地配置（避免泄露隐私）
# ==============================================
# 注意：当前项目的 config.py 是通用配置，无需忽略；
# 若有本地个性化配置（如含真实密码的 config_local.py），需添加以下规则：
# config_local.py
# *.db                 # 本地数据库文件（如 SQLite，你的项目用 MySQL 可忽略）

# ==============================================
# 4. 临时文件与日志（运行时生成，无版本价值）
# ==============================================
*.log                 # 日志文件
*.tmp                 # 临时文件
*.swp                 # Vim 编辑器交换文件
*.swo                 # Vim 编辑器交换文件（多轮编辑）
.DS_Store             # macOS 系统自动生成的隐藏文件
Thumbs.db             # Windows 系统缩略图缓存
desktop.ini           # Windows 目录配置文件

# ==============================================
# 5. 开发工具配置（IDE 本地设置，无需共享）
# ==============================================
.idea/                # PyCharm 配置目录
.vscode/              # VS Code 配置目录
*.iml                 # PyCharm 模块文件
*.suo                 # Visual Studio 解决方案用户选项
*.sublime-*           # Sublime Text 配置文件